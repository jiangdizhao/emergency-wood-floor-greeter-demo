# Copy this file to backend/.env for local development if you use python-dotenv,
# or set these variables directly in PowerShell before starting uvicorn.
# Never commit a real API key.

OPENAI_API_KEY=
OPENAI_TTS_MODEL=gpt-4o-mini-tts
OPENAI_TTS_VOICE=marin
